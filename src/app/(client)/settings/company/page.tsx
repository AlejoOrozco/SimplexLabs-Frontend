export default function CompanySettingsPage(): JSX.Element {
  return (
    <section>
      <h1 className="text-xl font-semibold">Company Settings</h1>
      <p className="mt-2 text-sm text-slate-600">
        Company-wide channels, defaults, and tenant-level operational preferences.
      </p>
    </section>
  );
}
