import { PageMeta } from '@/components/layout/page-meta';

export default function NotificationsPage(): JSX.Element {
  return (
    <section>
      <PageMeta title="Notifications" description="Alerts and updates about your workspace activity." />
    </section>
  );
}
